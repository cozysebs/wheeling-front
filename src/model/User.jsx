export default class User {
  constructor(username, password, name, tel, role, token, id) {
    this.username = username;
    this.password = password;
    this.name = name;
    this.tel = tel;
    this.role = role;
    this.token = token;
    this.id = id;
  }
}